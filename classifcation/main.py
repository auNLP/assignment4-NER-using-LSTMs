"""
The main script for reproducing the analysis

The comments are a suggestion, you are not required to structure it this way.
Another way to structure it would be function for each experiment.
"""



if __name__ == "__main__":
    # parse arguments
        # arguments look something like this --mdl logistic --tfidf --lr 0.01
    # pass arguments to
    # main(...)